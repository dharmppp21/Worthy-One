{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "signforge.serviceAccountName" . }}
  labels:
    {{- include "signforge.labels" . | nindent 4 }}
{{- end }}
