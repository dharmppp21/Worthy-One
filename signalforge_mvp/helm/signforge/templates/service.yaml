apiVersion: v1
kind: Service
metadata:
  name: {{ include "signforge.fullname" . }}
  labels:
    {{- include "signforge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "signforge.selectorLabels" . | nindent 4 }}
