apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "signforge.fullname" . }}-test-connection"
  labels:
    {{- include "signforge.selectorLabels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "signforge.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
